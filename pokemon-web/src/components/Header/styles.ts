import styled from 'styled-components';

export const Logo = styled.header`
    display: flex;
    justify-content: center;
    
    img {
        margin-top: 90px;
        width: 350px;
        height: auto;
    }       
`;

